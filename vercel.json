{
  "version": 2,
  "builds": [
    {
      "src": "app.js",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "npm install",
  "rewrites": [
    {
      "source": "/pages",
      "destination": "/"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/app.js" }
  ]
}
