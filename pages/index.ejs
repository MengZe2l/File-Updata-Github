<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Github图床</title>
    <link rel="stylesheet" href="https://cdn.bootcdn.net/ajax/libs/tailwindcss/2.2.19/tailwind.min.css">
    <link rel="stylesheet" href="https://cdn.bootcdn.net/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body class="bg-gradient-to-b from-blue-100 to-blue-50 text-gray-800 min-h-screen flex items-center justify-center">
    <div class="container mx-auto p-8 rounded-xl shadow-lg bg-white max-w-4xl">
        <h1 class="text-3xl font-bold mb-6 text-center text-blue-700"><i class="fas fa-folder-open"></i> GitHub 文件上传器</h1>
        <form action="/upload" method="POST" enctype="multipart/form-data" class="mb-8">
            <div class="flex flex-col md:flex-row items-center md:space-x-4 space-y-4 md:space-y-0">
                <input type="file" name="file" class="border border-gray-300 p-3 rounded-lg w-full focus:outline-none focus:ring-2 focus:ring-blue-300">
                <button type="submit" class="bg-blue-500 text-white px-6 py-3 rounded-lg hover:bg-blue-600 transition duration-300"><i class="fas fa-upload"></i> 上传文件</button>
            </div>
        </form>
        <h2 class="text-2xl font-semibold mb-4">仓库中的文件列表：</h2>
        <ul class="list-disc pl-5 space-y-2">
            <% files.forEach(file => { %>
                <li class="flex items-center space-x-2">
                    <i class="<%= file.type === 'file' ? 'fas fa-file text-blue-500' : 'fas fa-folder text-yellow-500' %> text-lg"></i>
                    <a href="<%= file.download_url %>" target="_blank" class="text-blue-600 hover:underline text-lg"><%= file.name %></a>
                </li>
            <% }); %>
        </ul>
    </div>
</body>
</html>
